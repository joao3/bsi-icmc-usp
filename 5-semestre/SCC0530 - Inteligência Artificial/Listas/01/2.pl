soma(X, Y, Z) :- Z is X + Y.

diferenca(X, Y, Z) :- Z is X - Y.

produto(X, Y, Z) :- Z is X * Y.

soma2(X, Y, W, Z) :- Z is X + Y + W.

diferenca2(X, Y, W, Z) :- Z is X - Y - W.

produto2(X, Y, W, Z) :- Z is X * Y * W.